import React from 'react';
import Styles from '../styles/components/footer.module.scss'

class Footer extends React.Component {
    render() {
        return (
            <footer className={Styles.footer}>Hayato OKUMA
            </footer>
        );
    }
}

export default Footer